[package]
name = "shared-positioning"
version = "0.1.0"
edition = "2021"

[dependencies]
nalgebra = "0.33"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }
rand = "0.8"
toml = "0.8"
serde_yaml = "0.9"

[dev-dependencies]
tempfile = "3.0"